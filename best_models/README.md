This folder contains best models per language.

Each model is trained on ai4bharat/IndicBERTv2-MLM-only or ai4bharat/IndicBERTv2-SS on batch size of 32 or 64 due to resource limitations.
However, increasing batch size to 128 may give better results.
